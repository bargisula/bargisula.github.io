# 專案開發與內容維護指南 (.antigravity.md)

這是本專案的維護指南，任何 AI 代理人在執行任務前必須閱讀並嚴格遵守。

## 1. 核心行為準則 (Behavioral Guidelines)
為了減少常見的開發錯誤，AI 代理人必須遵循以下準則。這些準則優先考慮「謹慎與正確」而非「執行速度」。

### A. 先思考，再動手 (Think Before Coding)
- **拒絕假設**：不要隱藏困惑。如果需求不明確，必須先詢問。
- **表面化權衡**：如果存在多種實作方案，請先列出並呈現，不要私自決定。
- **推動簡化**：如果存在更簡單的方法，應主動提出。必要時請對過度複雜的需求提出異議。
- **暫停機制**：如果任何細節不清楚，立即停止並明確指出困惑點，請求釐清。

### B. 簡潔至上 (Simplicity First)
- **最小可行方案**：只撰寫解決問題所需的最少程式碼。
- **拒絕過度工程**：
  - 不增加需求之外的功能。
  - 不為單次使用的程式碼建立抽象層。
  - 不增加未被要求的「彈性」或「可配置性」。
- **自我審核**：如果 50 行就能解決卻寫了 200 行，請重寫。問自己：「資深工程師會覺得這太複雜嗎？」如果是，請簡化。

### C. 外科手術式修改 (Surgical Changes)
- **精確打擊**：只觸動必須修改的部分。只清理自己造成的混亂。
- **不干擾原則**：
  - 不要「順便」改進相鄰的程式碼、註解或格式。
  - 不要重構沒有損壞的東西。
  - 嚴格遵守既有風格，即使你有不同的偏好。
- **孤立變更**：
  - 僅移除因「你的修改」而導致失效的 import、變數或函式。
  - 除非被要求，否則不移除預先存在的廢棄程式碼。
- **檢驗標準**：Diff 中的每一行修改都必須能直接溯源至使用者的原始請求。

### D. 目標導向執行 (Goal-Driven Execution)
- **定義成功準則**：將任務轉化為可驗證的目標（例如：先撰寫能重現 Bug 的測試，再修復它）。
- **制定簡短計畫**：針對多步驟任務，先列出步驟與驗證點：
  1. [步驟] → 驗證：[檢查項]
  2. [步驟] → 驗證：[檢查項]
- **持續循環**：直到所有驗證點通過，才視為任務完成。

---

## 2. 內容管理 (Astro Content Collections)
所有的筆記存放在 `src/content/notes/` 下。
- **建立新專題**：不能只建立資料夾。必須至少包含一個 `.md` 或 `.mdx` 檔案。
- **Frontmatter 規範**：每篇文章必須包含：`title`, `description`, `category`, `pubDate`。`subcategory` 與 `topic` 為**選填**，省略優於亂填。
- **禁止標題重複**：Markdown 正文中禁止出現 `# 標題` (H1)。
- **Topic 一致性**：`topic` 欄位必須與 `src/data/categories.ts` 中的 `CATEGORY_TREE` 完全一致。

## 3. UI 顯示邏輯 (核心規則)
本站分類卡片為**手動註冊制**。
- **新增專題卡片**：必須修改 `src/data/categories.ts` 內的 `CATEGORY_TREE`，將新專題名稱加入對應的 `topics` 陣列。**否則卡片不會出現在首頁。**（`src/pages/notes/index.astro` 只是 import，不在此修改）

## 4. Git 與部署規範
- **提交變更**：完成後執行 `git add [指定檔案]`（禁止 `git add .` 或 `git add -A`）、`git commit` 與 `git push`。
- **驗證部署**：確認 GitHub Actions (Build/Deploy) 成功。
- **禁止行為**：禁止擅自移動文章路徑；未更新 `CATEGORY_TREE` 前不得宣稱任務完成。

## 5. 常用快捷指令 (Quick Commands)
- **「寫台股快報」**：遵循 `scripts/taiwan-report-prompt.txt`。
- **「寫美股快報」**：遵循 `scripts/us-tech-analysis-prompt.txt`。
- **「執行技術分析」**：遵循 `scripts/tech-analysis-skill.md`。
- **「日報」(勞動、軍事、理財、不動產)**：
  - **流程**：搜尋時事 -> 彙整標題清單並完整列出 -> **停止，等待使用者明確回覆要選哪幾則，
    在得到使用者的選擇之前，絕對不可以產生摘要或檔案** -> 使用者選定後，才依選定的標題生成檔案。
  - **限制**：理財日報**嚴禁納入個股**。勞動日報聚焦台灣本地與爭議。
  - **不動產日報 category 路徑**（重要）：`category: '投資'` / `subcategory: '其他投資'` / `topic: '不動產'`。禁止使用 `category: '不動產'`，此值不存在於 schema enum。

## 6. 軍事內容分類原則 (核心規範)
- **區域防禦**：國際戰略、同盟演習、大國擴軍。
- **台海分析**：國軍部隊改編、演習、本土兵力部署、對美軍購。
- **武器裝備**：具體武器技術規格。
- **優先級**：混合時優先歸類於「區域防禦」。
